package main

func ProcessorReplace(markupToReplace string) (string, error) {
	// Build the side-request.
	//
	// Check other Turbodiv attributes, invoke Partitioners as needed on the request (In).
	//
	// Send the request.
	//
	// Invoke Partitioners on the response (Out).
}
